{
  "name": "@anitrend/configuration",
  "version": "1.0.0",
  "description": "Client configuration server",
  "type": "module",
  "scripts": {
    "dev": "deno run --import-map=../import_map.json --allow-net index.ts",
    "check": "deno check",
    "test": "deno test"
  }
}
